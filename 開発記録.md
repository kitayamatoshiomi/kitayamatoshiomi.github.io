# ベースサイト（Work Base）開発記録

## 2026-09-08　v1（初版）

検討メモ（2026-09-07 作成、Chat の壁打ち）を受けて着手。

**決定事項（北山さん）**
1. アーティファクト → GitHub Pages へ移行する。
2. Work Cosmos と 編集中 は 1 画面に統合する。ただし既存の 2 つ（アーティファクト版）はそのまま残す。
3. 並べる URL の数・グループ分けは作りながら検討。

**v1 で作ったもの**（`index.html`・自己完結 1 ファイル）
- HTML + SVG + CSS。中央の発光体から 6 グループのハブ、そこから 28 ツールへ放射状。曲線リンク＋グロー。
- グループ／ツールは Work Cosmos v19 の `TOOLS` 配列をそのまま移植（`authuser=0` 付き）。
  - Base / In Progress / Market / Competitors / Office / Other
- ラベルは英字。ホバーで日本語ツールチップ＋そのグループだけ点灯・他は減光（Obsidian のグラフビュー風）。
- 中央：自前描画の時計・日付・曜日。
- 中央の検索窓：`parent:19e1XLVpyfmhm... type:folder <語>` で Google ドライブ「編集中」直下を検索（編集中サイトのロジックを流用）。クイックリンク＝編集中フォルダ／刊行一覧。
- 右上「予定 & メモ」パネル：Google カレンダー iframe（AGENDA 表示）＋ Notion 作業板／タスクメモ／カレンダーを開く ボタン。既定は閉じている。
- 左下：localStorage メモ（端末ローカル、非同期）。
- 動き：イントロのドロー演出（リンクを描く→ノードが出る、約 2.5〜4 秒）→ 以降は静止。`prefers-reduced-motion` 対応。
- レイアウト調整：6 グループを 40° 回転させ真上・真下・水平の桟を回避。中央に UI 用の空白半径（`CORE_VOID`）を確保し、スポークはその外側から開始。`#core` オーバーレイは JS で SVG の中心に追従。

**未確定・要差し替え（CONFIG ブロック）**
- `CAL_SRC` = `kitayama@toyokan.co.jp`（カレンダーが表示されるか要確認。別アドレスなら差し替え）
- `NOTION_BOARD` / `NOTION_TASKS` = 仮（notion.so トップ）。実 URL を貼る。
- 並べる URL：現状 28。増減・グルーピングは今後。

**次にやること**
- GitHub リポジトリ作成（`kitayamatoshiomi-sudo`）→ Pages 有効化 → 固定 URL。
- 北山さんの反応を見てデザイン調整。
- カレンダー／Notion の実値を反映。

## 2026-09-22　v2（ダッシュボード化・GitHub Pages 準備）

北山さんの新しいイメージ：「仕事のベースサイト。頻繁に使うものをまとめ、必要な情報（通知）が入り込んでくる。ゆくゆくはオリジナル AI に」。Lenovo だけでなく Surface でも開けるよう GitHub で公開する。

**画面構成**
- 中央：Work Cosmos v19 の力学グラフをそのまま移植（デザイン重視）。常時動くように、ゆっくり回転（星に乗ると止まる）＋中心→ハブ→ツールへ信号が流れ続ける。
- コマンド（中央下の窓に入力して Enter）で発光：中心が膨らんで衝撃波リング、全スポークに光の信号。星の名前（英/日）が含まれていればその星とハブが光る。「〜を開いて」で 1 件なら開く。「全画面」「リセット」。AI は未接続（形だけ）。
- 左上：Notion 作業板「✅ 完了済み」の件数（初期値 485＝9/21 時点に MCP で数えた値）。「今日 +N」（history から算出）、「今日やること N」。クリックで Notion。
- 右上：教育／出版ニュース（タブ）。Google ニュース RSS を専門媒体中心に絞る：教育＝教育新聞・リセマム・教育家庭新聞＋文科省/中教審、出版＝新文化・文化通信・HON.jp＋出版社/書店。ノイズ媒体はブロックリスト。
- 左下：Gmail（一番上・強調）→ Calendar / Portal / 企画一覧 / 編集中 / 管理 / 企画書。上に「編集中」フォルダ検索窓（parent: 検索）。
- 右下：Amazon ランキング 6 ＋ honto、出版社 6。
- 四隅の行にカーソルを乗せると中央の対応する星が点灯、開くと光る。新しいニュース／完了数の増加が届くとカードが金色に光り、中心も発光（＝通知が流れ込む）。
- 全画面：⛶ ボタン／F キー／背景ダブルクリック。manifest（display: fullscreen）でアプリとしてインストール可。
- 860px 以下はカードを縦積み。

**自動更新**：`.github/workflows/update.yml` が毎時 `scripts/update.py` を実行して `data/*.json` をコミット。Notion は `NOTION_TOKEN` シークレット設定後に有効。

**バグ**：Drive の先頭行に付けた class `top` がヘッダーの `.top`（position:fixed）とぶつかり Gmail 行が浮いた → `first` に改名。


## 2026-09-25　v3（中央デザインの作り直し）

「編集中」artifact（0a94d311）の霧を移植し、中央を作り替えた。北山さんの確認を sample.html で取り、本体へ反映（sample.html は削除。必要ならこのコミットの1つ前から取り出せる）。

- 背景を差し替え：濃紺（#050810）＋淡い光のにじみ＋瞬く小星。
- 霧：ソフトなスプライトを重ねるボリューム霧（PUFF 150 / PUFF2 72）。流れ場（sin 合成）で渦を巻き、home へ弱いバネで戻る。中央に白く発光するモヤ（呼吸する放射グラデ）。
- 流動する粒子 MIST 620。外周の楕円を出たら中心付近で再生成。
- **星と星をつなぐ線・信号の演出は撤去**（力学シミュレーション自体をやめ、定位置＋ゆらぎに）。
- 星は球状（フィボナッチ球）配置＋透視投影。ドラッグで yaw/pitch を回し、慣性の後に自動回転へ復帰。奥の星は小さく沈め、霧を星の奥・手前の間に挟んで奥行きを出す。
- 星はふだん白（#e9eef9）。**クリックするとグループ色が付き、localStorage `wb-lit` に記憶**。ホバー中はそのグループだけ一時的に色が出る。「色リセット」で白に戻す。
- **R キーで球体表示**：半径520の球に星2600個（外側寄り）、霧を 12% まで落として引いた画角に。`wb-sphere` に記憶。「球体」コマンドでも切替。sph>0.97 では霧の計算をスキップ（軽量化）。
- manifest の色も #050810 に更新。

※ 中央の描画ブロックだけを差し替える形で作ったので、次に手を入れるときは index.html の「CENTER: 霧＋立体の星」ブロックを見る。
